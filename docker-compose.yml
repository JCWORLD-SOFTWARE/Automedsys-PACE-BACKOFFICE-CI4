version: "3.7"
services:
    automedsys-backoffice_app:
        build:
            context: .
        restart: unless-stopped
        volumes:
            - ./:/var/www/app
            - /var/www/app/vendor
        ports:
            - "8235:80"
        networks:
            - automedsys-backoffice_vpc

networks:
    automedsys-backoffice_vpc:
        external: true
